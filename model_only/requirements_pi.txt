# Requirements for Raspberry Pi 4B
# Install with: pip install -r requirements_pi.txt
# Note: picamera2 should be installed via apt: sudo apt install -y python3-picamera2

torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.5.0
numpy>=1.20.0
Pillow>=8.0.0
# picamera2 - install via apt: sudo apt install -y python3-picamera2
